# Design

Trace **statique** du design de sawadev (issue du handoff Claude Design). Le prototype
interactif (JSX/HTML, canvas, transcripts) a été retiré : la source de vérité vivante est
le code (`apps/web` + `apps/web/src/theme.css`).

- **[`index.html`](./index.html)** — récapitulatif : design system (palette, typographie,
  composants) + galerie des écrans clés. Ouvre-le dans un navigateur.
- **`screens/`** — captures de référence (login, IDE desktop/tablette/mobile, thème clair…).
